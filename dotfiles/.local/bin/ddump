#!/bin/bash -e

if [ -z "$1" ]; then
  echo "Usage: ddump <domain> [dir]"
  exit 1
fi

dir="${2:-"$PWD/subdomains"}"
mkdir -p "$dir"

amass enum -passive -d "$1" | while read -r subdomain; do
  dnsdump "$subdomain" > "$dir/$subdomain".dump &
done
